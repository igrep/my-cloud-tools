rewrite provision.sh with itamae
setup firewall and other security settings
  deploy the configuration files then restart.
    - /etc/iptables/iptables.rules
    - /etc/ssh/sshd_config
install Vim, Haskell, Dropbox, Hakyll, so on.
